import { ComingSoon } from "@/components/placeholder/coming-soon"
import { Target } from "lucide-react"

export const metadata = { title: "Estrategia" }

export default function StrategyPage() {
  return (
    <ComingSoon
      icon={Target}
      title="Estrategia (CoachMap)"
      description="7 outputs estratégicos por cliente: prospección, ángulos de comunicación, calendario de contenido, estructura de oferta, sales approach, landing page copy y closing angles."
      badge="En construcción — Fase 2"
    />
  )
}
